# 銀座ナイトワーク専用 POS システム設計書

> 対象業種: 銀座Bar / 銀座クラブ / 内地ワーク専門

---

## 1. システム全体アーキテクチャ

```
┌─────────────────────────────────────────────────────────────────┐
│                        クラウド層 (AWS / GCP)                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐  │
│  │  APIサーバー  │  │  DBサーバー  │  │  バックアップ/DR      │  │
│  │  (REST/gRPC) │  │ (PostgreSQL) │  │  (自動スナップショット)│  │
│  └──────┬───────┘  └──────┬───────┘  └──────────────────────┘  │
└─────────┼─────────────────┼───────────────────────────────────┘
          │ HTTPS/WSS        │
┌─────────┼─────────────────┼───────────────────────────────────┐
│         │    店舗内LAN      │                                   │
│  ┌──────▼───────┐  ┌──────▼───────────┐  ┌─────────────────┐ │
│  │  POSタブレット│  │ ローカルDBキャッシュ│  │  周辺機器       │ │
│  │  (iPad/Win)  │  │ (SQLite/IndexedDB)│  │  ・レシートPRT  │ │
│  └──────────────┘  └──────────────────┘  │  ・自動釣銭機   │ │
│                                           │  ・バーコードRDR│ │
│                                           │  ・決済端末     │ │
│                                           └─────────────────┘ │
└───────────────────────────────────────────────────────────────┘
```

---

## 2. データモデル（親子レコード構造）

```
[親レコード: 法人グループ]
  └─ group_id, group_name, owner_name, tax_id

      [子レコード: 店舗]
        └─ store_id, group_id(FK), store_name, store_type(bar/club/etc)
               store_address, phone, open_time, close_time

            [孫レコード: 営業日レポート (daily_report)]
              └─ report_id, store_id(FK), business_date
                   total_sales, net_sales, cash_sales, card_sales,
                   bottle_keep_sales, table_charge_total,
                   num_guests, num_tables,
                   staff_cost, drink_cost, misc_cost, gross_profit

            [孫レコード: 会計伝票 (receipt)]
              └─ receipt_id, store_id(FK), table_id, staff_id,
                   opened_at, closed_at, payment_method,
                   subtotal, service_charge, tax, total, discount

                  [曾孫: 伝票明細 (receipt_item)]
                    └─ item_id, receipt_id(FK), product_id(FK),
                         quantity, unit_price, line_total

            [孫レコード: ボトルキープ (bottle_keep)]
              └─ keep_id, store_id(FK), guest_name, product_id(FK),
                   opened_date, remaining_qty, label_memo

            [孫レコード: スタッフ戦績 (staff_performance)]
              └─ perf_id, store_id(FK), staff_id(FK), business_date,
                   shimei_count, dohan_count, sales_amount, back_percent

  [マスターテーブル群]
    ├─ products      (商品マスター: product_id, store_id, category, name, price, tax_type)
    ├─ categories    (カテゴリ: drinks/food/set/table_charge/service)
    ├─ staff         (スタッフ: staff_id, store_id, name, role, pin_hash)
    └─ tables        (テーブル: table_id, store_id, capacity, zone)
```

---

## 3. 機能要件 詳細

### 3-1. マスター登録（商品・料金設定）

| 項目 | 内容 |
|------|------|
| 商品カテゴリ | ドリンク / フード / セット料金 / テーブルチャージ / サービス料 |
| 価格設定 | 通常価格 / 時間帯別価格（ハッピーアワーなど） |
| 税区分 | 標準10% / 軽減8% / 非課税 |
| ボトル管理 | 銘柄・容量・仕入れ値・販売価格 |
| 一括インポート | CSVアップロードで大量登録対応 |

### 3-2. 日次経営ダッシュボード（デイリーレポート）

```
┌─────────────────────────────────────────────────────┐
│  営業日: 2026-05-31  /  店舗: GINZA BAR HIKARI      │
├──────────────────┬──────────────────────────────────┤
│  売上サマリー     │  コスト内訳                      │
│  総売上    ¥480,000│  人件費    ¥95,000              │
│  純売上    ¥436,364│  飲料原価  ¥72,000              │
│  (消費税   ¥43,636)│  雑費      ¥15,000              │
│                   │  合計コスト¥182,000              │
├──────────────────┼──────────────────────────────────┤
│  決済内訳         │  KPI                            │
│  現金      ¥180,000│  粗利益率  62.1%               │
│  カード    ¥240,000│  客単価    ¥16,000              │
│  電子決済   ¥60,000│  席回転数  1.8回               │
├──────────────────┴──────────────────────────────────┤
│  配管費用（固定費）: ¥35,000  ← 家賃按分/水道光熱費 │
└─────────────────────────────────────────────────────┘
```

**集計される経営指標一覧:**

| 指標名 | 計算式 |
|--------|--------|
| 総売上 | 全会計合計（税込） |
| 純売上 | 総売上 ÷ 1.1（税抜） |
| 準売上 | ボトルキープ消化分 + サービス料除外ベース |
| 粗利益 | 純売上 − 飲料原価 |
| 営業利益 | 粗利益 − 人件費 − 配管費用（家賃・光熱費等） |
| 客単価 | 総売上 ÷ 来客数 |
| テーブル単価 | 総売上 ÷ テーブル数 |
| 指名率 | 指名件数 ÷ 来客組数 |
| 同伴率 | 同伴件数 ÷ 来客組数 |

### 3-3. 複数店舗連結ビュー

```
[グループダッシュボード]
  ├─ GINZA BAR HIKARI    売上 ¥480,000  利益率 62.1%  ▲前週比+5%
  ├─ GINZA CLUB LUNA     売上 ¥920,000  利益率 58.3%  ▼前週比-2%
  └─ CLUB STELLA         売上 ¥640,000  利益率 60.7%  ▲前週比+8%
  ─────────────────────────────────────────────────────
  合計                  売上¥2,040,000  加重利益率 60.2%
```

---

## 4. 非機能要件 対応設計

### 4-1. 可用性 — オフライン対応

```
オンライン時:  POS → APIサーバー → クラウドDB（リアルタイム同期）
オフライン時:  POS → ローカルキャッシュDB（SQLite）
                         ↓ 復旧後
              ローカルDB → 差分同期キュー → クラウドDB（自動マージ）
```

- **Service Worker** + **IndexedDB** でブラウザベースPOSのオフライン対応
- オフライン中の全トランザクションを`sync_queue`テーブルに保存
- 接続復旧時に`created_at`順でべき等マージ

### 4-2. 性能・拡張性

| 要件 | 対応 |
|------|------|
| 商品表示 0.5秒以内 | 商品マスターをローカルキャッシュ（起動時プリロード） |
| ピーク対応 | クラウド側オートスケーリング（ECS / Cloud Run） |
| DB負荷分散 | 読み取りレプリカ分離（Write: Primary / Read: Replica） |
| CDN | 静的アセットはCDN配信でUI高速化 |

### 4-3. セキュリティ

```
権限レベル:
  オーナー    → 全店舗データ閲覧 / 設定変更 / 売上修正
  店長        → 自店舗全機能 / スタッフ管理
  チーフ      → 会計・伝票操作 / レポート閲覧
  スタッフ    → 会計入力のみ（修正・削除は申請制）
  会計士      → 読み取り専用（確定済みデータのみ）
```

- ログイン: PIN + 生体認証（TouchID / FaceID）
- 決済データ: **AES-256暗号化** + PCI DSS準拠
- 通信: **TLS 1.3**のみ許可
- 監査ログ: 全操作を`audit_log`テーブルに記録（削除不可）
- セッションタイムアウト: 非操作15分で自動ロック

### 4-4. 運用・保守性

| 機能 | 仕様 |
|------|------|
| 自動バックアップ | 15分ごとにクラウドスナップショット、30日保持 |
| 自動アップデート | 営業終了後の深夜2〜4時に自動適用（ロールバック機能付き） |
| 障害通知 | サービス異常時にオーナー・店長へPush通知 + Slack連携 |
| データ復旧 | 任意の時点へのPITR（Point-in-Time Recovery） |
| ヘルスチェック | 5分ごとに死活監視、異常時自動再起動 |

### 4-5. 互換性・外部連携

```
周辺機器 (USB/Bluetooth/LAN):
  ├─ レシートプリンター  → ESC/POSプロトコル対応
  ├─ 自動釣銭機          → GLORY/Glory製標準API
  ├─ バーコードリーダー  → HID入力として透過処理
  └─ キャッシュレス端末  → GMO / Square / PAX端末SDK

外部システム連携:
  ├─ 会計ソフト  → freee / MFクラウド (CSV/API)
  ├─ 在庫管理    → 売上時に在庫テーブル自動デクリメント
  └─ Airレジ等   → 標準POSデータフォーマット出力
```

---

## 5. 画面構成（主要画面）

```
[1] ホーム/テーブル選択  →  テーブルの空席/使用中ステータス一覧
[2] 注文入力             →  カテゴリ選択 → 商品タップ → 数量指定
[3] 伝票確認             →  明細一覧 / 割引 / ボトルキープ適用
[4] 会計処理             →  支払方法選択 → 釣銭計算 → レシート印刷
[5] 日次レポート         →  本日の全KPI + グラフ
[6] 複数店舗ダッシュボード→  グループ全店比較表
[7] マスター管理         →  商品/スタッフ/テーブル登録・編集
[8] 設定                 →  権限管理 / 周辺機器設定 / バックアップ
```

---

## 6. 技術スタック（推奨）

| レイヤー | 技術 |
|----------|------|
| フロントエンド (POS画面) | React + TypeScript + PWA (オフライン対応) |
| バックエンド API | Node.js (Fastify) または Python (FastAPI) |
| データベース | PostgreSQL（クラウド） + SQLite（ローカルキャッシュ） |
| 認証 | JWT + リフレッシュトークン, PIN/生体認証 |
| クラウド基盤 | AWS (ECS Fargate + RDS Aurora + S3 + CloudFront) |
| 監視 | Datadog / CloudWatch |
| CI/CD | GitHub Actions → Staging → 自動テスト → 本番 |

---

## 7. 開発フェーズ案

| フェーズ | 期間 | 内容 |
|----------|------|------|
| Phase 1 | 2ヶ月 | マスター登録 / 会計・伝票基本機能 / レシート印刷 |
| Phase 2 | 2ヶ月 | 日次レポート / オフライン対応 / 決済端末連携 |
| Phase 3 | 1.5ヶ月 | 複数店舗連結ビュー / スタッフ戦績 / ボトルキープ |
| Phase 4 | 1ヶ月 | 外部会計連携 / セキュリティ強化 / 運用テスト |
